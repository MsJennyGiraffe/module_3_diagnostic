Name, Address, Fuel Types, Distance, and Access Times

<% @stations.each do |station| %>
  <div class="station">
    <%= station.name %>
    <%= station.address %>
    <%= station.fuel_type %>
    <%= station.distance %>
    <%= station.access_times %>
  </div>
<% end %>
